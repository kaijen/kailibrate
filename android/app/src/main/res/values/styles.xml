<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch theme for the activity, set on the activity's "theme" attribute in AndroidManifest.xml -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when Flutter draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Normal theme for the activity, set on the activity's "meta-data" -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
